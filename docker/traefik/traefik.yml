api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: petclinic-frontend

log:
  level: INFO

accessLog: {}

ping:
  entryPoint: web
