spring:
  cloud:
    config:
      enabled: false
  ai:
    openai:
      api-key: test-key
      chat:
        options:
          temperature: 0.7
          model: gpt-4o-mini

eureka:
  client:
    enabled: false
